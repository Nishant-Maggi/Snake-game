class Pellet{
    constructor(){
        this.generateFood();
    }

    generateFood(){
        if(pellet === null){
        var pellet = createSprite(Math.round(random(1,29))*20,Math.round(random(1,29))*20,10,10);
        pellet.shapeColor = "black";
        //console.log("pellet");
        }
    }

    display(){
        
        
    }
}